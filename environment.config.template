# =============================================================================
# ImmoAssist Multi-Agent System Environment Configuration Template
# =============================================================================

# ==============================
# GOOGLE CLOUD AUTHENTICATION  
# ==============================

# Recommended: Use Application Default Credentials (ADC)
# The system automatically detects your project using google.auth.default()
# Run: gcloud auth application-default login
GOOGLE_GENAI_USE_VERTEXAI=True

# ⚠️  AVOID setting these unless absolutely necessary:
# Setting GOOGLE_CLOUD_PROJECT can conflict with Vertex AI auto-detection
# Only uncomment if ADC detection fails:
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=europe-west1

# Alternative: AI Studio API Key (Development/Testing only)
# Uncomment the following lines to use AI Studio instead of Vertex AI:
# GOOGLE_GENAI_USE_VERTEXAI=False
# GOOGLE_API_KEY=your-ai-studio-api-key

# ==============================
# MODEL CONFIGURATION
# ==============================

# Gemini model to use (default: gemini-2.5-pro)
MODEL_NAME=gemini-2.5-pro

# ==============================
# RAG (RETRIEVAL AUGMENTED GENERATION) 
# ==============================

# Optional: Vertex AI RAG corpus for knowledge base
# RAG_CORPUS=projects/your-project/locations/your-location/ragCorpora/your-corpus

# ==============================
# ADK SERVER CONFIGURATION
# ==============================

# Server port (default: 8000)
PORT=8000

# Debug mode (default: True for development)
DEBUG=True

# ==============================
# MULTI-AGENT SYSTEM FEATURES
# ==============================

# Enable/disable individual agents
ENABLE_KNOWLEDGE_AGENT=true
ENABLE_PROPERTY_AGENT=true
ENABLE_CALCULATOR_AGENT=true
ENABLE_ANALYTICS_AGENT=true

# Session management
SESSION_TIMEOUT_MINUTES=60
ENABLE_SESSION_PERSISTENCE=true

# ==============================
# LOGGING AND MONITORING
# ==============================

LOG_LEVEL=INFO
ENABLE_ANALYTICS_TRACKING=true

# ==============================
# SECURITY BEST PRACTICES
# ==============================

# For production deployment:
# 1. Use VPC Service Controls for network isolation
# 2. Configure IAM roles with least privilege principle
# 3. Enable audit logging for compliance
# 4. Use private Google access for secure connectivity

# ==============================
# SETUP INSTRUCTIONS
# ==============================

# 1. For Vertex AI (Recommended for production):
#    - Install Google Cloud SDK: https://cloud.google.com/sdk/docs/install
#    - Run: gcloud auth application-default login
#    - Enable required APIs:
#      * gcloud services enable aiplatform.googleapis.com
#      * gcloud services enable storage.googleapis.com
#    - System auto-detects project ID and uses appropriate region

# 2. For AI Studio (Development only):
#    - Get API key from https://aistudio.google.com/app/apikey
#    - Set GOOGLE_GENAI_USE_VERTEXAI=False
#    - Set GOOGLE_API_KEY=your-key

# 3. Optional RAG Setup:
#    - Create a RAG corpus in Vertex AI
#    - Upload your knowledge base documents
#    - Set RAG_CORPUS to the corpus resource name

# 4. Production Security:
#    - Configure VPC Service Controls
#    - Set up private service connectivity
#    - Enable Cloud Audit Logs
#    - Configure appropriate IAM roles

# 5. Copy this file to .env and update values as needed
# 6. Run: adk web --port=8000 